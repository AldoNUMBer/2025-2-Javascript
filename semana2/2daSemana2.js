// Clase 1: ¿Qué es programar? + Variables y Tipos de Datos

const { ask } = require('../helpers/input');

async function main() {

    opcion = await ask('Qué deseas hacer' /n1. agregar una fruta /n2. Eliminar una fruta /n3. Salir)
    if (opcion === 1){
        //agregar
        let frutaNueva = await ask('¿Qué fruta desea agregar?: ');
        frutas
    }



    console.log('Frutas')
    continue;
    } else {
        let frutasEliminar await ask('¿Qué fruta deseas eliminar?')
        let index = frutasEliminar.indexOf(frutasEliminar);

        if(index !== -1){
            frutasEliminar.slplice(index, 1)
        }
    






    
}



main();